# Core
pydantic>=2.7
PyYAML>=6.0
typer>=0.12

# Graph store
neo4j>=5.20,<6.0

# PDF extraction (pdfplumber, not PyMuPDF - see docs/02_architecture.md SS5)
pdfplumber>=0.11

# Excel read/write
openpyxl>=3.1

# google-genai, not anthropic: budget-driven choice, Gemini's free tier
# (Google AI Studio, no credit card) has no ongoing cost, unlike the
# Anthropic API which requires a paid credit purchase with no free
# tier as of this writing. Explicitly permitted by the assignment
# brief ("any frontier API"). See src/extraction/llm.py's docstring.
google-genai>=0.3

# Testing
pytest>=8.2

# Deliberately NOT included: pandas (float64 conflicts with the Decimal-only
# numeric policy in docs/00_metric_catalog.md), FastAPI, NetworkX — see
# docs/02_architecture.md §5 for why.
